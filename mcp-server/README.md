# MCP Server README

这是一个基于 FastMCP 的邮件发送服务。

## 安装

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```

## 使用

配置 `.env` 文件，包含 SMTP 服务器信息。

## 功能

- 创建周报 Excel
- 发送邮件
